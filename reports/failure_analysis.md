# Failure Analysis

## False Positives

### Case 1

- What features pushed the model toward its (wrong) decision?
- Does the feature pattern make sense, or does it expose something the model doesn't understand?
- What would you change to fix this specific case?

### Case 2

- What features pushed the model toward its (wrong) decision?
- Does the feature pattern make sense, or does it expose something the model doesn't understand?
- What would you change to fix this specific case?

## False Negatives

### Case 1

- What features pushed the model toward its (wrong) decision?
- Does the feature pattern make sense, or does it expose something the model doesn't understand?
- What would you change to fix this specific case?

### Case 2

- What features pushed the model toward its (wrong) decision?
- Does the feature pattern make sense, or does it expose something the model doesn't understand?
- What would you change to fix this specific case?

## Overall Trade-off

Given the 4 failure cases, is the model currently biased toward false positives or false negatives, and is that the right trade-off for a real fraud system?
